---
"chat-adapter-backlog": minor
---

Implement `handleWebhook` for `BacklogAdapter` to process Backlog issue-updated webhook events containing comments, converting them into Chat SDK `Message` objects.
